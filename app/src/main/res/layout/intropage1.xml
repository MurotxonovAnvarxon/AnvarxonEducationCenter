<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAF4E1">


    <ImageView
        android:id="@+id/backYel"
        android:layout_width="300sp"
        android:layout_height="300sp"
        android:layout_marginTop="80sp"
        android:src="@drawable/elips"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="MissingConstraints" />

    <ImageView
        android:id="@+id/imageintro"
        android:layout_width="200sp"
        android:layout_height="200sp"
        android:src="@drawable/intro1"
        app:layout_constraintBottom_toBottomOf="@id/backYel"
        app:layout_constraintLeft_toLeftOf="@id/backYel"
        app:layout_constraintRight_toRightOf="@id/backYel"
        app:layout_constraintTop_toTopOf="@id/backYel" />

    <TextView
        android:id="@+id/introtext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to \n Anvarxon's Education Center"
        android:textColor="#000000"
        android:gravity="center"
        android:textSize="25sp"
        android:layout_marginTop="30sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/backYel" />

</androidx.constraintlayout.widget.ConstraintLayout>